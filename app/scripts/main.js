$(document).ready(function(){
	'use strict';
	$('.owl-carousel').owlCarousel({
	    autoplay:true,
	    autoplaySpeed: 800,
	    autoplayTimeout: 8000,
	    autoplayHoverPause:true,
	    loop:true,
	    autoWidth: true
	});
});