$(document).ready(function(){
	'use strict';
	$('.owl-carousel').owlCarousel({
	    autoplay:10000,
	    paginationSpeed: 40000,
	    autoplayHoverPause:true,
	    loop:true,
	    items: 4,
	    slideSpeed: 100,
	   	autoWidth: true
	});
});